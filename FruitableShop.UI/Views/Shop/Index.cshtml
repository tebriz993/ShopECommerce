@model ShopECommerce.UI.ViewModels.OurProducts_VM

<!-- Single Page Header start -->
<div class="container-fluid page-header py-5">
    <h1 class="text-center text-white display-6">Shop</h1>
    <ol class="breadcrumb justify-content-center mb-0">
        <li class="breadcrumb-item"><a href="#">Home</a></li>
        <li class="breadcrumb-item"><a href="#">Pages</a></li>
        <li class="breadcrumb-item active text-white">Shop</li>
    </ol>
</div>
<!-- Single Page Header End -->
<!-- Fruits Shop Start -->
<div class="container-fluid fruite py-5">
    <div class="container py-5">
        <div class="row g-4">
            <div class="col-lg-12">
                <div class="row g-4">
                </div>
                <div class="row g-4">
                    <div class="col-lg-3">
                        <div class="row g-4">
                            <div class="col-lg-12">
                                <div class="mb-3">
                                    <h4>NamesOfTypes</h4>
                                   @*  @foreach (var namesOfTypes in Model)
                                    {
                                        <ul class="list-unstyled fruite-categorie">
                                            <li>
                                                <div class="d-flex justify-content-between fruite-name">
                                                    <a asp-controller="Shop" asp-action="Index" asp-route-shopNamesOfTypesId="@namesOfTypes.Id"><i class="fas fa-apple-alt me-2"></i>@namesOfTypes.TypeName</a>
                                                    <span>@namesOfTypes.Products.Count()</span>
                                                </div>
                                            </li>
                                        </ul>
                                    } *@
                                </div>
                            </div>

                            <div class="col-lg-12">
                                <div class="mb-3">
                                    <h4>Additionals</h4>
                                    @* @foreach (var namesOfTypes in Model.Shop_Additionals)
                                    {
                                        <ul class="list-unstyled fruite-categorie">
                                            <li>
                                                <div class="d-flex justify-content-between fruite-name">
                                                    <a asp-controller="Shop" asp-action="Index" asp-route-shop_AdditionalId="@namesOfTypes.Id"><i class="fas fa-apple-alt me-2"></i>@namesOfTypes.Type</a>
                                                    <span>@namesOfTypes.Products.Count()</span>
                                                </div>
                                            </li>
                                        </ul>
                                    } *@
                                </div>
                            </div>

                            <div class="col-lg-12">
                                <h4 class="mb-3">Featured products</h4>
                                @* @foreach (var featuredProducts in Model)
                                {
                                    <div class="d-flex align-items-center justify-content-start">
                                        <div class="rounded me-4" style="width: 100px; height: 100px;">
                                            <img src="~/assets/img/@featuredProducts.Image" class="img-fluid rounded" alt="">
                                        </div>
                                        <div>
                                            <h6 class="mb-2">@featuredProducts.ProductName</h6>
                                            <div class="d-flex mb-2">

                                                @for (int i = 0; i < featuredProducts.Stars; i++)
                                                {
                                                    <i class="fa fa-star text-secondary"></i>
                                                }

                                                @for (int i = 0; i < (5-featuredProducts.Stars); i++)
                                                {
                                                    <i class="fa fa-star"></i>
                                                }
                                                
                                                
                                            </div>
                                            <div class="d-flex mb-2">
                                                <h5 class="fw-bold me-2">@featuredProducts.NewPrice $</h5>
                                                <h5 class="text-danger text-decoration-line-through">@featuredProducts.BeforePrice $</h5>
                                            </div>
                                        </div>
                                    </div>
                                }
 *@
                                <div class="d-flex justify-content-center my-4">
                                    <a href="#" class="btn border border-secondary px-4 py-3 rounded-pill text-primary w-100">View More</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-9">
                        <div class="row g-4 justify-content-center">
                            @* @foreach (var product in Model.ProductViewDto)
                            {
                                <div class="col-md-6 col-lg-6 col-xl-4">
                                    <div class="rounded position-relative fruite-item">
                                        <div class="fruite-img">
                                            <img src="~/assets/img/@product.CategoryImage" class="img-fluid w-100 rounded-top" alt="">
                                        </div>
                                        <div class="text-white bg-secondary px-3 py-1 rounded position-absolute" style="top: 10px; left: 10px;">Fruits</div>
                                        <div class="p-4 border border-secondary border-top-0 rounded-bottom">
                                            <h4>@product.Title</h4>
                                            <p>@product.Subtitle</p>
                                            <div class="d-flex justify-content-between flex-lg-wrap">
                                                <p class="text-dark fs-5 fw-bold mb-0">@product.SignOfMoney</p>
                                                <p class="text-dark fs-5 fw-bold mb-0">@product.Price /</p>
                                                <p class="text-dark fs-5 fw-bold mb-0">@product.TypeOfWeight</p>
                                                <a href="#" class="btn border border-secondary rounded-pill px-3 text-primary"><i class="fa fa-shopping-bag me-2 text-primary"></i> Add to cart</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            } *@

                            <div class="col-12">
                                <div class="pagination d-flex justify-content-center mt-5">
                                    <a href="#" class="rounded">&laquo;</a>
                                    <a href="#" class="active rounded">1</a>
                                    <a href="#" class="rounded">2</a>
                                    <a href="#" class="rounded">3</a>
                                    <a href="#" class="rounded">4</a>
                                    <a href="#" class="rounded">5</a>
                                    <a href="#" class="rounded">6</a>
                                    <a href="#" class="rounded">&raquo;</a>
                                </div>
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Fruits Shop End -->
